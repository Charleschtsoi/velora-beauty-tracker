/**
 * When true, barcode lookup and AI photo analysis are skipped so the app can be
 * run for academic submission without incurring external API costs. The user is
 * sent to the Add Product form to enter details manually.
 * Set to false to re-enable for production use.
 */
export const SUBMISSION_DISABLE_PAID_APIS = true;
