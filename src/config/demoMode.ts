/**
 * Demo mode for investor demos: when true, scan (barcode or AI photo) uses
 * predefined demo products and auto-adds to inventory without calling external APIs.
 * Set to false for production or submission (e.g. academic submission).
 */
export const DEMO_MODE = false;
