import type { ImageSourcePropType } from 'react-native';

/**
 * Home hero banner (K-beauty editorial). Replace the file at
 * `src/assets/images/home-hero-kbeauty.png` to swap the image.
 */
export const HOME_HERO_IMAGE: ImageSourcePropType = require('./images/home-hero-kbeauty.png');
