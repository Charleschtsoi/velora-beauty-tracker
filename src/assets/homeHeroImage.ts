import type { ImageSourcePropType } from 'react-native';

/**
 * Home hero banner (K-beauty editorial). Replace `hero-banner.png` to swap the image.
 */
export const HOME_HERO_IMAGE: ImageSourcePropType = require('./images/hero-banner.png');
